// A helper script to verify logic before dropping into the html.
// Here I'll author the 3d engine and geometry functions.
const faces = [];
function addFace(pts, fill, stroke) {
  faces.push({ pts, fill, stroke });
}
//...
